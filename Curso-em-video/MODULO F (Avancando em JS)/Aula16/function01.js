function ParImp(n){
    if (n % 2 == 0) {
        return `${n} = PAR`
    }else{
        return `${n} = ÍMPAR`
    }
}

//console.log(ParImp(4)) => Pode fazer a chamada dessa forma.

/* como dessa forma também
let numero = ParImp(4)
console.log(numero)
*/